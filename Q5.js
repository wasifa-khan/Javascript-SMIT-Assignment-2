function changeBackgroundColor(element, color) {
    element.style.backgroundColor = color;
  }
  function changeColor() {
    let heading = document.querySelector('#main-heading');
    changeBackgroundColor(heading, 'Green');
  }

  function changeBackgroundColor(element, color) {
    element.style.backgroundColor = color;
  
  }